# dm_tgui_type_bridge

This tool generates TypeScript types for TGUI by querying dm-langserver to
retrieve type metadata from the codebase. This allows DM to remain the
source of truth.

Currently used for:
 - /datum/preference
